# ═══════════════════════════════════════════════════════════════════════════
# Azure OpenAI — 2 secrets required
# ═══════════════════════════════════════════════════════════════════════════
#
# The scaffold pre-commits AZURE_OPENAI_DEPLOYMENT and AZURE_OPENAI_API_VERSION
# in .replit [env], so only the 2 sensitive values below are needed.
#
# On Replit: save them once in your Account Secrets (top-right avatar →
#   Account → Account Secrets), then in each remix click the Secrets pane →
#   "Link Account Secrets". Never committed, never copied on remix by default.
#
# Locally: copy this file to back/.env (already gitignored) and fill in values.
#
# ───────────────────────────────────────────────────────────────────────────

# Your Azure OpenAI resource endpoint.
# Example: https://my-resource.openai.azure.com/
AZURE_OPENAI_ENDPOINT=

# API key from Azure Portal → your OpenAI resource → Keys and Endpoint.
AZURE_OPENAI_API_KEY=

# ═══════════════════════════════════════════════════════════════════════════
# Non-secret defaults (already set in .replit — override here if needed)
# ═══════════════════════════════════════════════════════════════════════════
# AZURE_OPENAI_DEPLOYMENT=gpt-5-chat
# AZURE_OPENAI_API_VERSION=2025-01-01-preview

# ═══════════════════════════════════════════════════════════════════════════
# Alternative auth — DefaultAzureCredential (local dev only, not Replit)
# ═══════════════════════════════════════════════════════════════════════════
# Leave AZURE_OPENAI_API_KEY empty and run `az login` in your terminal.
# Requires Azure role: "Cognitive Services OpenAI User" on the resource.

# ═══════════════════════════════════════════════════════════════════════════
# Azure Document Intelligence (optional — OCR for scanned PDFs)
# ═══════════════════════════════════════════════════════════════════════════
# AZURE_DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com/
# FORCE_DOC_INTELLIGENCE=true
