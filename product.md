# Product — _À compléter_

<!--
How to structure this file:
- Keep it short (1 page). The Product Manager agent uses this to generate the backlog.
- Focus on the WHY and WHAT, not the HOW.
- Replace every `_À compléter_` below with your own content.

Tip: the Product Manager prompt in `.agents/prompts/` can walk you through filling this in.
-->

## Vision

_À compléter_ — one paragraph describing what the agent does and who it helps.
Example shape: "A _N_-step AI agent that helps _{users}_ accomplish _{outcome}_ by _{mechanism}_."

## Users

**Primary:** _À compléter_ (role, level)
**Context:** _À compléter_ (when / where they use it)
**Frequency:** _À compléter_ (daily / weekly / one-off)

## Problem solved

_À compléter_ — what pain does this remove, and by how much?
Quantify if you can (e.g. "reduces a 30-min task to 2 min").

## Core workflow

1. _À compléter_ — user action
2. _À compléter_ — agent response (Step 1)
3. _À compléter_ — user edit / review
4. _À compléter_ — agent response (Step 2)
5. _À compléter_ — final outcome

## Output format

_À compléter_ — structured JSON? Markdown? What fields?
Example: "Step 1 returns `{summary, key_points[]}`; Step 2 returns `{recommendations[], next_steps[], conclusion}`."

## Constraints

- **Language:** _À compléter_ (FR / EN / both)
- **Input:** _À compléter_ (text only? files? form?)
- **Output:** _À compléter_ (on-screen only? exportable?)
- _À compléter_ (other constraints — no external integrations, no multi-user, etc.)

## Success criteria

- _À compléter_ — measurable (latency, quality, adoption)
- _À compléter_
