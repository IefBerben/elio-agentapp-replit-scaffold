# Elio Scaffold — Project Memory

> Agent reads and updates this file throughout the build.
> Do not delete sections — mark them with status instead.

---

## Project

**Name:** Consulting Assistant *(replace with your app name)*
**Status:** template — reference app installed, your app not yet built

---

## Stack

| Layer | Tech |
|-------|------|
| Backend | FastAPI · Python 3.12 · LangChain · uv |
| Frontend | React 18 · Vite · TypeScript · Zustand · Tailwind CSS |
| LLM | Azure OpenAI via `get_llm()` — `back/services/llm_config.py` |
| Auth | 4 env vars required — 2 shipped as defaults, 2 secrets the consultant provides |

### Required env vars on first run

All 4 env vars below are required at runtime. The scaffold ships committed defaults for the 2 non-sensitive ones in `.replit [env]`, so the consultant only needs to provide the 2 sensitive Secrets.

| Env var | Sensitive | How it's supplied | Where to find / override |
|---------|-----------|-------------------|--------------------------|
| `AZURE_OPENAI_ENDPOINT` | Yes | Replit Secret (consultant provides) | Azure Portal → your OpenAI resource → Keys and Endpoint |
| `AZURE_OPENAI_API_KEY` | Yes | Replit Secret (consultant provides) | Azure Portal → same page |
| `AZURE_OPENAI_DEPLOYMENT` | No | Committed default in `.replit` (`gpt-5-chat`) | Override in Configurations pane if your deployment has a different name |
| `AZURE_OPENAI_API_VERSION` | No | Committed default in `.replit` (`2025-01-01-preview`) | Override in Configurations pane if your deployment needs another version |

**Recommended Secrets flow** — set once in Replit Account Secrets, link on every remix:
1. Replit top-right avatar → **Account** → **Account Secrets** → add `AZURE_OPENAI_API_KEY` + `AZURE_OPENAI_ENDPOINT`.
2. In this workspace → **🔒 Secrets** pane → **Link Account Secrets** → link both.

If either is missing, the agent MUST request them via the Replit Secrets flow before running any code. Do not hardcode or guess values.

When `AZURE_OPENAI_DEPLOYMENT` is set (it is, via `.replit`), **all** `get_llm()` calls route to that single deployment, regardless of the model name passed.

---

## Paths

```
back/                          Python backend
  agents/_reference/           Protected reference — never modify
  agents/{your_agent}/         Your agent (created by Agent)
  services/llm_config.py       LLM factory — always use get_llm()
  services/process_files.py    File extraction (PDF, DOCX, PPTX, XLSX, audio)
  services/generate_files.py   Document generation (DOCX, PPTX)
  main.py                      FastAPI app + AGENTS_MAP

front/                         React frontend
  src/pages/                   One page per agent app
  src/components/agent-apps/   Elio UI library — use these components only
  src/stores/agent-apps/       Zustand stores — one per agent
  src/i18n/locales/            fr.json + en.json

packages/shared-types/src/     TypeScript DTOs shared between front and back
.agents/docs/                  Elio v3 platform guidelines (read-only, Agent only)
.agents/docs/api-contracts.md  Generated by generate-api-contracts skill
```

---

## Reference implementation (pre-installed)

A working consulting assistant demonstrating all platform patterns:

| Layer | File | Description |
|-------|------|-------------|
| Backend step 1 | `back/agents/_reference/step1_generate.py` | Prompt → JSON (summary + key points) |
| Backend step 2 | `back/agents/_reference/step2_refine.py` | Key points → recommendations + next steps |
| Models | `back/agents/_reference/models.py` | Pydantic input/output models |
| Prompts | `back/agents/_reference/prompt_fr.py` + `prompt_en.py` | Bilingual prompt constants |
| Frontend page | `front/src/pages/_ReferencePage.tsx` | Full 2-step UI with SSE streaming |
| Store | `front/src/stores/agent-apps/_referenceStore.ts` | Zustand + persist + partialize |
| AGENTS_MAP keys | `_reference-step-1` · `_reference-step-2` | Registered in `back/main.py` |

**Never modify the `_reference` files.** They are the protected pattern example.

---

## App being built

*Agent fills this section after running `intake-from-markdown`.*

**Use case:** —
**Actors:** —
**Core workflow:** —
**Steps:** —
**API surface:** —
**File upload:** —
**Document generation:** —
**Bilingual:** —

---

## API contracts

*Generated by `generate-api-contracts` skill into `.agents/docs/api-contracts.md` and `packages/shared-types/src/index.ts`.*

Status: not yet generated

---

## Build progress

*Agent updates this table after each skill completes.*

| Feature | Skill | Status |
|---------|-------|--------|
| API contracts | generate-api-contracts | ⬜ pending |
| Backend | build-backend | ⬜ pending |
| Frontend | build-frontend | ⬜ pending |
| Platform check | platform-integration-check | ⬜ pending |

---

## Blocked items

*None yet.*

---

## Session log

*Agent appends a one-line entry after each significant action.*

| Date | Action |
|------|--------|
| — | Template initialized |
